// 1. Keep the URL completely clean (no ?limit=... at the end)
var url = "http://[REAL_HOSPITAL_IP]/api/rest/diagnosis";

// 2. Package the variables as true numbers in a JSON object
var requestBody = new { 
    limit = limit, 
    offset = offset 
};

var jsonContent = new StringContent(
    System.Text.Json.JsonSerializer.Serialize(requestBody), 
    System.Text.Encoding.UTF8, 
    "application/json"
);

// 3. Send it as a POST request
var response = client.PostAsync(url, jsonContent).Result;
